<?xml version="1.0" encoding="utf-8"?>
<ru.rambler.libs.swipe_layout.SwipeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:background="@drawable/corner_ll"
    android:layout_marginBottom="1dp"
    android:layout_height="230dp"
    android:id="@+id/root"
    app:left_swipe_enabled="true">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="230dp">

<androidx.cardview.widget.CardView
    android:id="@+id/item_fishka_card_root"
    android:layout_width="wrap_content"
    android:layout_height="180dp"
    android:paddingLeft="20dp"
    android:layout_marginEnd="20dp"
    android:layout_marginTop="20dp"
    android:layout_marginStart="20dp"
    android:clickable="true"
    android:foreground="?selectableItemBackground"
    android:elevation="5dp"
    app:cardElevation="5dp"
    app:cardCornerRadius="7dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="180dp"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/item_fishka_card_image"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:src="@mipmap/img_fishcard9"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/item_fishka_card_number"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="20dp"
            android:layout_marginBottom="20dp"
            android:text="574697390624059436"
            android:textColor="@color/black"
            android:textSize="18sp"
            android:layout_alignParentRight="true"
            android:layout_alignParentBottom="true"/>


    </RelativeLayout>
</androidx.cardview.widget.CardView>
    </RelativeLayout>

    <RelativeLayout
        android:layout_width="140dp"
        android:layout_marginTop="20dp"
        android:layout_height="200dp"
        app:gravity="right"
        android:paddingTop="20dp"
        app:bring_to_clamp="140dp"
        app:clamp="self"
        app:sticky="1400dp">



        <RelativeLayout
            android:layout_width="140dp"
            android:layout_height="match_parent"
            android:id="@+id/rlDeleteItem"

            android:background="@color/red">


            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerHorizontal="true"
                android:layout_centerInParent="true"
                android:layout_marginTop="14dp"
                android:textStyle="bold"
                android:text="@string/delete"
                android:textAllCaps="true"
                android:textColor="@color/white"
                android:textSize="14sp" />

        </RelativeLayout>

    </RelativeLayout>

</ru.rambler.libs.swipe_layout.SwipeLayout>